/*
 * Table Block
 * Recreate a table
 * https://www.hlx.live/developer/block-collection/table
 */

export default async function decorate(block) {
  const pic = col.querySelector('ul');
  console.log(pic);
}
